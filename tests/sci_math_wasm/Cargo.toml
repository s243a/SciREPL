[package]
name = "sci_math"
version = "0.1.0"
edition = "2021"
description = "A toy Rust WASM library for SciREPL — add, multiply, fibonacci, factorial, stats"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1"

[profile.release]
opt-level = "s"
lto = true
